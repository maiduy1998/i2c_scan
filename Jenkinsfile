
Jenkinsfile (Declarative Pipeline)

pipeline {
    agent any 
    stages {
        stage('clone') {
            steps {
                git 'https://github.com/maiduy1998/i2c_scan.git'
            }
        }
    }
}

